# cypress-demo

#### Installation and execution

```bash
git clone https://github.com/danrusu/cypress-demo.git
cd cypress-demo
npm install
npm run cy:open
```

#### Parallel tests demo

```bash
docker build -t demo .
docker-compose up
```
